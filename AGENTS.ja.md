# AGENTS.md

このファイルは、リポジトリルートで作業するエージェント向けの実践ガイドです。
より深い階層に別の `AGENTS.md` がある場合、その配下のファイルにはそちらが優先されます。

## 1. スコープ

- このリポジトリは、複数の APM パッケージをまとめた monorepo 形式のインデックスです。
- パッケージ固有の変更の多くは `packages/<name>/` 配下にあります。
- `apm.yml`、`README.md`、`README.ja.md`、および共有リポジトリ設定は、リポジトリ全体に適用されます。

## 2. ドキュメント同期

- 文書にローカライズ版がある場合は、意味と構成を揃えて同期してください。
- これは次のような組み合わせに適用されます。
  - `*.md` と `*.ja.md`
  - 英語版と日本語版の README
  - 英語版と日本語版の skill reference、template、spec
- 片方の言語で実質的な内容を変更した場合、可能であれば同じ変更の中で他方も更新してください。
- ソース文書が変わったのにローカライズ版が中途半端に残る状態を避け、追加・削除・並び順の変更もできるだけ揃えてください。
- ローカライズ版が意図的に遅れている場合は、その不一致が見えるように要約で明示してください。

## 3. 変更の流れ

- 編集前に既存の文書とパッケージ固有ガイドを読む。
- 動作と文書の整合性を保てる最小差分を優先する。
- パッケージコードを編集したら、ユーザー向け動作が変わる場合は `packages/**/.apm/skills/**` の配布ドキュメントも更新する。
- 完了を宣言する前に、必要な検証やテストを実行する。

## 4. Ignore / 整理

- 生成キャッシュやローカル作業ディレクトリは version control に含めない。
- 一時生成物が繰り返し現れる場合は `.gitignore` に追加する。
- `.pnpm-store/` のようなワークスペース専用キャッシュをコミットしない。

